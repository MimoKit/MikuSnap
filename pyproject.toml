[project]
name = "gs-miku-snap"
version = "1.3.1"
description = "GsCore 初音未来主题网页自动截图插件：自动识别网页链接，深色模式渲染，GitHub 仓库卡片解析，跳过视频/直链后用 Playwright 截图。"
requires-python = ">=3.10"
dependencies = [
    "httpx[socks]>=0.27.0",
    "playwright>=1.40.0",
    "Pillow>=10.0.0",
]
