[project]
name = "gs-miku-snap"
version = "1.2.0"
description = "GsCore 网页自动截图插件（初音未来主题）：自动识别网页链接，跳过视频/直链后用 Playwright 截图。"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.25.0",
    "playwright>=1.40.0",
    "Pillow>=10.0.0",
]
