# 贡献指南

欢迎提交 Issue 和 Pull Request。

## 提交要求

- Issue、Pull Request 标题和内容请尽量使用中文；
- 代码注释优先使用中文；
- 新增配置项时，请同步更新 `_conf_schema.json` 和 `README.md`；
- 新增功能时，请同步更新 `CHANGELOG.md`；
- 提交前请确保 `main.py` 能通过 Python 编译检查。

## 建议检查命令

```bash
python -m py_compile main.py
```
